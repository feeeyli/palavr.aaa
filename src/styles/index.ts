import { createStitches } from "@stitches/react";

export const { styled } = createStitches({
	theme: {
		colors: {
			right: "#3dae9b",
			place: "#d59a67",
			wrong: "#2d2a32",
			slotBorder: "#47434c",
			slotLocked: "#58535f",
			background: "#645d6f",
			white: "#fcfaff",
		},
	},
});
